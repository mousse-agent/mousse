// Designated chat SDK folder — all chat UI types/components/helpers live here
// UI path: provider (pi, claude-cli, codex-cli) -> standardize output -> ui types -> build ui

export * from './adapters/mousseToUI'

// Agent-elements primitives (vendored, adapted for Electron/React19)
// Import via 'renderer/chat/components/agent-elements/*' to avoid scattering
